#include "findPal.h"

int is_pal() {
    while (/* condition */)
    {
        if (/*reversed word is in dict*/)
        {
            /* code */
            return 1; // Is a pal
        }
        
    }
    return 0; // Is not a pal
}